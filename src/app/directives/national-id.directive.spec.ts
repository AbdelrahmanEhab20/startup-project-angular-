import { NationalIdDirective } from './national-id.directive';

describe('NationalIdDirective', () => {
  it('should create an instance', () => {
    const directive = new NationalIdDirective();
    expect(directive).toBeTruthy();
  });
});
