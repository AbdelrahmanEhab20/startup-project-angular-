import { NotAllowHtmlDirective } from './not-allow-html.directive';

describe('NotAllowHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new NotAllowHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
